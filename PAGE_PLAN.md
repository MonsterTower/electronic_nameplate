# 电子桌牌后续开发计划

本文档用于记录接下来几步的页面、数据和网络接入计划，方便开发过程中随时对照。

## 当前目标

当前阶段的重点不是最终视觉设计，而是打通数据链路：

- 能正确获取信息。
- 能把信息保存到统一数据模型。
- 页面只读取数据模型并显示。
- 按键可以切换 0 到 3 四个页面。
- 为后续真实 4.2 寸墨水屏、低功耗和重新排版预留接口。

## 页面渲染原则

1. 每次绘制页面前，先清理或重建显示缓冲区，再绘制固定布局和动态内容。
2. 显示逻辑只读取数据模型，不直接负责联网、按键处理和电池采样。
3. 模块之间只做数据耦合，不做逻辑耦合。
4. 字符串过长时要截断、换行，必要时缩小局部字体。
5. 页面切换时可以全屏刷新，局部数据变化时预留低功耗刷新接口。
6. 页面由按钮状态切换，按钮模块只维护状态值，页面模块不直接读取按钮 GPIO。

## 模块分层计划

### 1. 数据模型层

新增统一数据模型，例如 `app_model.h/.c`。

职责：

- 汇总按钮、电池、Wi-Fi、时间、课程、桌牌等数据。
- 给页面层提供只读快照。
- 后续网络 JSON、Flash 缓存、默认数据都写入模型或模型来源模块。

建议结构：

```c
typedef struct {
    nameplate_info_t nameplate;
    calendar_info_t calendar;
    course_table_t courses;
    battery_info_t battery;
    network_info_t network;
    time_info_t time;
} app_model_t;
```

页面层只接收：

```c
void display_pages_show(uint8_t page_id, const app_model_t *model);
```

### 2. 汉字渲染层

先采用轻量级 UTF-8 点阵子集字库。

短期方案：

- 支持 ASCII。
- 支持当前页面会用到的一小批汉字。
- 未收录字符显示为方框或问号。
- 字模以 16x16 或 12x12 单色点阵保存。

需要支持的第一批汉字：

- 姓名、单位、会议、主题、日期、星期、天气、课程、教室、电量、联网、时间、未校准、厦门大学。

后续如果需要动态录入任意中文，再考虑接入完整 GB2312/常用汉字点阵库。

暂不优先使用 LVGL、FreeType 等重型方案。

### 3. 页面层

重写或扩展 `display_pages.c/.h`，让 0 到 3 页面都通过数据模型绘制。

页面 0：电子桌牌

- 姓名。
- 单位。
- 会议主题。
- 二维码区域先预留，短期可显示 URL 或占位框。

页面 1：电子日历

- 日期。
- 星期。
- 天气。
- 今日日程。
- 角落保留电量、时间、Wi-Fi 状态。

页面 2：电子课程表

- 显示当天课程列表。
- 突出显示下一节课。
- 显示课程名、时间、教室。
- 角落保留电量、时间、Wi-Fi 状态。

页面 3：状态页

- 突出显示当前电量。
- 显示 Wi-Fi 连接状态、IP、NTP 状态。
- 显示固件版本。
- 显示最近刷新时间或错误信息。

### 4. 页面切换

按钮模块继续维护 0 到 3 的循环状态。

主控层负责：

- 调用 `input_panel_update()`。
- 检测页面状态是否变化。
- 状态变化时调用页面刷新。
- 页面模块不直接调用按钮模块。

目标流程：

```text
按钮 -> input_panel state -> 主控层检测变化 -> display_pages_show(page, model)
```

### 5. Wi-Fi 与 JSON 数据

电子桌牌、天气、课程等信息后续通过 Wi-Fi 录入或拉取。

建议 JSON 格式：

```json
{
  "nameplate": {
    "name": "张三",
    "org": "厦门大学",
    "topic": "电子设计与工艺实训",
    "qr": "https://example.com/checkin"
  },
  "weather": {
    "city": "厦门",
    "text": "多云",
    "temp": 28
  },
  "courses": [
    {
      "name": "嵌入式系统",
      "start": "10:10",
      "end": "11:45",
      "room": "海韵教学楼204"
    }
  ]
}
```

实现建议：

- 短期先用固定 URL 拉取 JSON，验证数据链路。
- 网络失败时保留上一次有效数据。
- JSON 解析完成后只更新数据模型，不直接刷新页面。
- 后续可升级为配置门户、HTTP POST 或本地 Web 配置页。
- HTTP/JSON 功能建议保留宏开关，便于控制固件大小：

```c
#define NETWORK_ENABLE_HTTP_JSON 1
```

### 6. 刷新策略

短期：

- 页面切换使用全屏刷新。
- 数据变化后可以先手动触发全屏刷新。

预留接口：

```c
typedef enum {
    DISPLAY_REFRESH_NONE,
    DISPLAY_REFRESH_PARTIAL,
    DISPLAY_REFRESH_FULL,
} display_refresh_mode_t;
```

后续低功耗阶段：

- 页面切换全刷。
- 时间、电量、Wi-Fi 状态小范围变化尽量局部刷新。
- 网络失败时不持续刷新屏幕。
- 深睡前保存必要数据。

## 建议执行顺序

1. 新增 `app_model` 数据模型，先填充默认测试数据。
2. 新增轻量中文点阵渲染能力。
3. 重写页面 0 到 3，让页面只读取 `app_model_t`。
4. 接入按钮状态，实现页面切换。
5. 把电池、Wi-Fi、NTP 数据写入模型，并显示到页面角落或状态页。
6. 重新引入 HTTP/JSON，先拉取桌牌、天气、课程测试数据。
7. 增加 Flash 缓存，网络失败时继续显示上一次有效数据。
8. 再进入低功耗状态机设计。

## 当前验收标准

近期验收只看功能链路：

- 四个页面都能显示。
- 按钮能切换页面。
- 页面 1 到 2 能显示时间、电量、Wi-Fi 状态。
- 页面 3 能清楚显示电量和联网状态。
- 中文能显示常用字段。
- NTP 未同步时显示“时间未校准”，不显示错误日期。
- 字符串过长时不越界、不覆盖关键内容。
